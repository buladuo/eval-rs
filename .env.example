# =============================================================================
# eval-rs 环境变量配置
# 复制本文件为 .env 并填入实际值: cp .env.example .env
#
# 环境变量格式: EVAL__<section>__<field>=<value>
# 双下划线 __ 表示嵌套层级，可覆盖 config/default.toml 中的任意字段
# =============================================================================

# -----------------------------------------------------------------------------
# Provider API 密钥（必须）
# -----------------------------------------------------------------------------

# GLM provider（默认 provider）
EVAL_PROVIDERS__GLM__API_KEY=sk-your-glm-api-key

# OpenAI provider（如需启用，在 config/default.toml 中取消注释对应段落）
# EVAL_PROVIDERS__OPENAI__API_KEY=sk-your-openai-api-key

# Anthropic provider（如需启用）
# EVAL_PROVIDERS__ANTHROPIC__API_KEY=sk-ant-your-anthropic-api-key

# -----------------------------------------------------------------------------
# 服务器配置（可选）
# -----------------------------------------------------------------------------

# EVAL_SERVER__HOST=0.0.0.0
# EVAL_SERVER__PORT=8080

# -----------------------------------------------------------------------------
# 限流配置（可选）
# -----------------------------------------------------------------------------

# EVAL_LIMITS__MAX_CONCURRENCY=10
# EVAL_LIMITS__REQUESTS_PER_MINUTE=60
# EVAL_LIMITS__TOKENS_PER_MINUTE=90000
# EVAL_LIMITS__TOKENS_PER_SECOND=1500

# -----------------------------------------------------------------------------
# 评测配置（可选）
# -----------------------------------------------------------------------------

# EVAL_EVAL__TIMEOUT_SECS=120

# -----------------------------------------------------------------------------
# 存储配置（可选）
# -----------------------------------------------------------------------------

# EVAL_STORAGE__ENABLED=true
# EVAL_STORAGE__DB_PATH=data/eval.db

# -----------------------------------------------------------------------------
# 日志配置（可选）
# -----------------------------------------------------------------------------

# EVAL_LOG__LEVEL=info
# EVAL_LOG__FORMAT=text
